# PhantomGit - Python package requirements
#
# Install with:
#   pip install -r requirements.txt
#
# Note: install.py also installs these automatically based on your choices
# during setup (AI provider and change-detection mode), so manual
# installation is optional.

# --- Required (always) ---
requests>=2.28.0
psutil>=5.9.0

# --- Recommended (for "watch" or "hybrid" change-detection mode) ---
# This is what makes PhantomGit react in seconds rather than minutes.
# If you choose "polling" mode at setup, this is not needed.
watchdog>=3.0.0

# --- Optional (only if you choose Google Gemini as the AI provider) ---
# Uncomment the line below, or let install.py install it for you.
# google-genai>=0.3.0

# Note: OpenAI-compatible providers (OpenAI, Ollama, LM Studio, llama.cpp, vLLM)
# and Anthropic are reached via plain HTTP using `requests`, so no extra
# SDKs are required for them.