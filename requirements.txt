# PhantomGit - Required Python packages
#
# Install with:
#   pip install -r requirements.txt
#
# Note: install.py also installs these automatically based on the chosen
# AI provider, so manual installation is optional.

# --- Required (always) ---
requests>=2.28.0
psutil>=5.9.0

# --- Optional (only if you choose Google Gemini as the AI provider) ---
# Uncomment the line below, or let install.py install it for you.
# google-genai>=0.3.0

# Note: OpenAI-compatible providers (OpenAI, Ollama, LM Studio, llama.cpp, vLLM)
# and Anthropic are reached via plain HTTP using `requests`, so no extra
# SDKs are required for them.